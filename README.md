# ระบบเข้าเวรผ่าน Discord (Local Database)

บอท Discord สำหรับระบบเข้าเวร บันทึกข้อมูลลงฐานข้อมูล **SQLite ในเครื่อง** แบบเรียลไทม์ — ไม่ต้องพึ่ง Google Sheets, Google Cloud หรือ Service Account อีกต่อไป ทุกอย่างทำงานอยู่ภายในบอทเอง

## โครงสร้างโปรเจกต์

```
duty-bot/
├── config.json                  # role แอดมิน, ห้อง log, รายชื่อหน่วยงาน
├── .env.example                 # ตัวอย่างไฟล์ตั้งค่า (คัดลอกเป็น .env)
├── data/
│   └── duty.db                  # ไฟล์ฐานข้อมูล SQLite (สร้างอัตโนมัติเมื่อรันครั้งแรก)
└── src/
    ├── index.js                 # จุดเริ่มต้นของบอท
    ├── deploy-commands.js       # สคริปต์ลงทะเบียน slash commands
    ├── commands/
    │   ├── register.js          # /สมัคร
    │   ├── checkin.js           # /เข้าเวร
    │   ├── checkout.js          # /ออกเวร
    │   ├── hours.js             # /ชั่วโมง
    │   └── admin/
    │       ├── onDuty.js        # /คนเข้าเวร
    │       ├── clearDuty.js     # /ล้างเวร
    │       ├── editTime.js      # /แก้เวลา
    │       ├── addHours.js      # /เพิ่มชั่วโมง
    │       ├── subHours.js      # /ลดชั่วโมง
    │       ├── weeklySummary.js # /สรุปสัปดาห์
    │       └── exportData.js    # /ส่งออกข้อมูล (สำรองข้อมูลเป็นไฟล์ CSV ส่งในดิสคอร์ด)
    └── utils/
        ├── db.js                 # เชื่อมต่อ + สร้างตาราง SQLite ทั้งหมด
        ├── time.js                # คำนวณเวลา/ชั่วโมง รองรับเข้าเวรข้ามวัน
        ├── embeds.js              # สร้าง Discord Embed
        └── permissions.js         # เช็คสิทธิ์แอดมิน + ส่ง log
```

## ขั้นตอนติดตั้ง

### 1. ติดตั้ง dependencies

```bash
npm install
```

> ใช้ `node:sqlite` ซึ่งเป็นโมดูล SQLite ที่**มากับ Node.js เองตั้งแต่ 22.5 ขึ้นไป** ไม่ต้องติดตั้งไลบรารีฐานข้อมูลแยก และไม่ต้องคอมไพล์ native module (ไม่ต้องมี Visual Studio Build Tools / node-gyp เหมือน `better-sqlite3`) ต้องใช้ **Node.js เวอร์ชัน 22.5 ขึ้นไป** — เช็คด้วย `node -v`
>
> เวลารันอาจเห็นข้อความ `ExperimentalWarning: SQLite is an experimental feature` ซึ่งเป็นแค่คำเตือน ไม่กระทบการทำงาน

### 2. สร้าง Discord Bot

1. ไปที่ https://discord.com/developers/applications → New Application
2. ไปที่แท็บ **Bot** → กด Reset Token เพื่อได้ `DISCORD_TOKEN`
3. เปิด **Privileged Gateway Intents** → เปิด `SERVER MEMBERS INTENT`
4. ไปที่แท็บ **OAuth2 → URL Generator** เลือก scope `bot` และ `applications.commands` พร้อมสิทธิ์ที่จำเป็น (Send Messages, Embed Links, Attach Files) แล้วใช้ลิงก์เชิญบอทเข้าเซิร์ฟเวอร์
5. คัดลอก **Application ID** เป็น `DISCORD_CLIENT_ID` และ ID เซิร์ฟเวอร์เป็น `DISCORD_GUILD_ID` (เปิด Developer Mode ใน Discord แล้วคลิกขวาที่เซิร์ฟเวอร์ > Copy Server ID)

### 3. ตั้งค่าไฟล์ .env

```bash
cp .env.example .env
```

`.env` ตอนนี้มีแค่ค่าที่เกี่ยวกับ Discord ล้วนๆ ไม่มีค่า Google อีกต่อไป:

```
DISCORD_TOKEN=...
DISCORD_CLIENT_ID=...
DISCORD_GUILD_ID=...
DB_PATH=./data/duty.db
```

### 4. ตั้งค่า config.json

- `adminRoleIds` — ใส่ Role ID ของแอดมิน (คลิกขวาที่ role ในเซิร์ฟเวอร์ > Copy Role ID)
- `logChannels` — ใส่ Channel ID ของห้อง log แต่ละประเภท (ถ้าไม่ตั้งค่าจะข้ามการส่ง log อัตโนมัติ)
- `departments` — รายชื่อหน่วยงานที่จะให้เลือกตอน `/สมัคร`

### 5. ลงทะเบียน Slash Commands

```bash
npm run deploy
```

### 6. รันบอท

```bash
npm start
```

เมื่อรันครั้งแรก ไฟล์ `data/duty.db` และตารางทั้งหมดจะถูกสร้างอัตโนมัติผ่าน `utils/db.js` — ไม่ต้องเตรียมอะไรล่วงหน้า

## โครงสร้างฐานข้อมูล (แทนที่ 3 แท็บเดิมใน Google Sheets)

**ตาราง `members`** (แทน `Members`)
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| discord_id | TEXT PRIMARY KEY | |
| discord_name | TEXT | |
| ingame_name | TEXT | ชื่อในเกม |
| department | TEXT | หน่วยงาน |
| position | TEXT | ตำแหน่ง |
| registered_at | TEXT | วันที่สมัคร (ISO datetime) |

**ตาราง `duty_log`** (แทน `Duty Log`)
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | |
| discord_id | TEXT | |
| checkin_at | TEXT | ISO datetime เต็ม |
| checkout_at | TEXT NULL | ISO datetime เต็ม, ว่างถ้ายังไม่ออกเวร |
| total_hours | REAL NULL | คำนวณตอนออกเวร |
| status | TEXT | `กำลังเข้าเวร` / `ออกแล้ว` / `ปรับเพิ่ม` / `ปรับลด` |

**ตาราง `summary`** (แทน `Summary`, cache สำหรับ `/สรุปสัปดาห์` และระบบสรุป/รีเซ็ตรายสัปดาห์อัตโนมัติ)
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| discord_id | TEXT PRIMARY KEY | |
| hours_month | REAL | ของเดือนล่าสุดที่รัน `/สรุปสัปดาห์` (เก็บไว้เผื่อใช้ในอนาคต ไม่ได้ใช้แสดงผลหลักแล้ว) |
| shift_count | INTEGER | |
| updated_at | TEXT | |

**ตาราง `applications`** (ใบสมัครเข้าหน่วยงาน ดูหัวข้อ "ระบบใบสมัคร (Application System)")
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | |
| discord_id | TEXT | ผู้สมัคร |
| discord_name | TEXT | |
| department | TEXT | หน่วยงานที่สมัคร |
| game_name | TEXT | ชื่อในเกม |
| reason | TEXT | เหตุผลที่อยากเข้าร่วม |
| experience | TEXT NULL | ประสบการณ์/ข้อมูลเพิ่มเติม |
| status | TEXT | `รอตรวจสอบ` / `อนุมัติ` / `ปฏิเสธ` |
| reviewed_by | TEXT NULL | Discord ID ของผู้อนุมัติ/ปฏิเสธ |
| reviewed_at | TEXT NULL | ISO datetime |
| review_channel_id / review_message_id | TEXT NULL | ตำแหน่งข้อความในห้องผู้อนุมัติ (ไว้แก้ไขปุ่มหลังตัดสิน) |
| created_at | TEXT | ISO datetime |

## รายการคำสั่งทั้งหมด

**สมาชิกทั่วไป**
| คำสั่ง | หน้าที่ |
|---|---|
| `/สมัคร` | สมัครสมาชิก (ห้ามสมัครซ้ำ) |
| `/เข้าเวร` | เริ่มเข้าเวร (แบบพิมพ์คำสั่ง) |
| `/ออกเวร` | สิ้นสุดเวร คำนวณชั่วโมงอัตโนมัติ (แบบพิมพ์คำสั่ง) |
| `/ชั่วโมง` | ดูชั่วโมงของตัวเอง (วันนี้/สัปดาห์/เดือน/จำนวนครั้ง) ผ่าน Embed |
| `/ใบสมัคร` | เปิดเมนูปุ่มเลือกหน่วยงานที่ต้องการสมัคร แล้วกรอกแบบฟอร์มใบสมัคร (ห้ามสมัครซ้ำ/สมัครซ้อนถ้ามีใบที่รออยู่) |
| 🟢/🔴 ปุ่ม **เข้าเวร / ออกเวร** บนแผงเข้าเวร | ทำงานเหมือน `/เข้าเวร` และ `/ออกเวร` ทุกประการ แต่กดปุ่มแทนพิมพ์คำสั่ง |

**แอดมิน** (ต้องมี role ตามที่ตั้งใน `config.json`)
| คำสั่ง | หน้าที่ |
|---|---|
| `/แผงเข้าเวร [ห้อง]` | โพสต์ "แผงเข้าเวร" แบบมีปุ่ม 🟢 เข้าเวร / 🔴 ออกเวร ในห้องที่ระบุ (ค่าเริ่มต้นคือห้องที่รันคำสั่ง) |
| `/แผงสมัคร [ห้อง]` | โพสต์ "แผงสมัคร" แบบมีปุ่มเลือกหน่วยงาน ค้างไว้ถาวรในห้องที่ระบุ (ค่าเริ่มต้นคือห้องที่รันคำสั่ง) — ใครก็กดสมัครได้ตลอดเวลาโดยไม่ต้องพิมพ์ `/ใบสมัคร` |
| `/คนเข้าเวร` | แสดงรายชื่อที่ยังไม่ออกเวร (query จาก `duty_log`) |
| `/ล้างเวร @สมาชิก` | ล้างสถานะเข้าเวรค้าง |
| `/แก้เวลา` | แก้เวลาเข้า/ออกของรายการเวรที่ระบุวันที่ |
| `/เพิ่มชั่วโมง` | เพิ่มชั่วโมงด้วยตนเอง (พร้อมระบุเหตุผล) |
| `/ลดชั่วโมง` | ลดชั่วโมงด้วยตนเอง (พร้อมระบุเหตุผล) |
| `/สรุปสัปดาห์` | สรุปชั่วโมงทุกคนของสัปดาห์นี้ และเขียนลงตาราง `summary` (ระบบยังสรุป+รีเซ็ตอัตโนมัติทุกต้นสัปดาห์ให้เองด้วย ดูหัวข้อ "ระบบสรุป/รีเซ็ตรายสัปดาห์อัตโนมัติ") |
| `/ส่งออกข้อมูล` | ส่งออกข้อมูลทั้ง 3 ตารางเป็นไฟล์ `.csv` แนบมาในข้อความดิสคอร์ด (ใช้แทนการเปิดดูชีต) |

## แผงเข้าเวรแบบปุ่ม (Duty Panel)

บอทมีระบบ "แผงเข้าเวร" เป็นข้อความปักหมุดพร้อมปุ่ม 🟢 **เข้าเวร** / 🔴 **ออกเวร** และแสดงสถานะแบบเรียลไทม์ว่าตอนนี้มีใครกำลังเข้าเวรอยู่บ้าง (จำนวนคน + รายชื่อ) เหมือนภาพตัวอย่างที่ให้มา

**วิธีติดตั้ง:**
1. ให้แอดมิน (มี role ตาม `adminRoleIds` ใน `config.json`) รันคำสั่ง `/แผงเข้าเวร` ในห้องที่ต้องการ (หรือระบุห้องผ่าน option `ห้อง`)
2. บอทจะโพสต์ข้อความแผงเข้าเวร (3 embed ซ้อนกัน: หัวข้อ, คำอธิบาย, สถานะ) พร้อมปุ่ม แล้วบันทึกตำแหน่งข้อความไว้ในตาราง `duty_panel`
3. ทุกครั้งที่มีคนกด 🟢 เข้าเวร หรือ 🔴 ออกเวร (หรือใช้คำสั่ง `/เข้าเวร`, `/ออกเวร`, หรือแอดมินใช้ `/ล้างเวร`) บอทจะแก้ไข (edit) ข้อความแผงเข้าเวรให้ตรงกับสถานะล่าสุดโดยอัตโนมัติ — ไม่ต้องรีเฟรชเอง
4. เวลากดปุ่ม ผู้ใช้จะได้รับข้อความตอบกลับแบบ ephemeral (เห็นคนเดียว) เหมือนตอนใช้สแลชคอมมานด์ทุกประการ (แจ้งข้อผิดพลาดถ้ายังไม่สมัครสมาชิก, เข้าเวรซ้ำ, ยังไม่ได้เข้าเวร ฯลฯ)
5. หากบอทออฟไลน์แล้วกลับมาออนไลน์ใหม่ จะซิงก์แผงเข้าเวรที่ปักไว้ให้ตรงกับฐานข้อมูลล่าสุดโดยอัตโนมัติตอนบอทพร้อมทำงาน (`ready` event)
6. ต้องการย้ายแผงไปห้องอื่น หรือรีเซ็ตดีไซน์ ก็แค่รัน `/แผงเข้าเวร` ซ้ำอีกครั้ง — จะโพสต์ข้อความใหม่และให้บอทติดตามข้อความนั้นแทน (ข้อความเก่าจะไม่ถูกลบอัตโนมัติ ลบเองได้ตามต้องการ)

## ระบบใบสมัคร (Application System)

ระบบให้สมาชิกทั่วไปสมัครเข้าหน่วยงานด้วยตนเองผ่านปุ่ม โดยใบสมัครจะถูกส่งไปให้ **ห้องผู้อนุมัติ** ตรวจสอบและกดอนุมัติ/ปฏิเสธแยกจากห้องที่สมาชิกใช้สมัคร

มีให้ใช้งาน 2 แบบ เลือกใช้แบบใดแบบหนึ่งหรือทั้งคู่พร้อมกันก็ได้ (ปุ่ม `form_apply_...` ที่กดได้ผลลัพธ์เหมือนกันทุกประการ ไม่ว่าจะมาจากที่ไหน):

- **แบบพิมพ์คำสั่ง** — สมาชิกพิมพ์ `/ใบสมัคร` แล้วบอทตอบกลับแบบ ephemeral (เห็นคนเดียว)
- **แบบแผงปักหมุด (เหมือนแผงเข้าเวร)** — แอดมินรัน `/แผงสมัคร [ห้อง]` เพื่อโพสต์ "แผงสมัคร" เป็นข้อความค้างถาวรในห้องที่กำหนด สมาชิกกดปุ่มสมัครได้ตลอดเวลาโดยไม่ต้องพิมพ์คำสั่ง เหมือนกับแผงเข้าเวร (บันทึกตำแหน่งข้อความไว้ในตาราง `application_panel` และซิงก์ให้ตรงกับ `departments` ล่าสุดใน `config.json` อัตโนมัติทุกครั้งที่บอทออนไลน์)

**วิธีติดตั้ง:**
1. ตั้งค่า `applicationChannelId` ใน `config.json` ให้เป็น Channel ID ของ **ห้องผู้อนุมัติ** (ห้องที่ให้เฉพาะแอดมิน/ผู้อนุมัติเห็น) — คลิกขวาที่ห้อง > Copy Channel ID
2. (ไม่บังคับ) ตั้งค่า `logChannels.ใบสมัคร` ถ้าต้องการให้ส่ง log ใบสมัคร/ผลการอนุมัติไปอีกห้องหนึ่งด้วย
3. รัน `npm run deploy` เพื่อลงทะเบียนคำสั่ง `/ใบสมัคร` ใหม่ (ถ้ายังไม่เคยลงทะเบียน)

**วิธีทำงาน:**
1. สมาชิกที่ยังไม่ได้อยู่ในระบบพิมพ์คำสั่ง `/ใบสมัคร` ที่ห้องไหนก็ได้ที่บอทมองเห็น — บอทจะตอบกลับแบบ ephemeral (เห็นคนเดียว) เป็น **เมนูปุ่ม** ให้เลือกหน่วยงานที่ต้องการสมัคร (ดึงรายชื่อจาก `departments` ใน `config.json`)
2. กดปุ่มหน่วยงานที่ต้องการ → บอทเปิดฟอร์ม (Modal) ให้กรอก **ชื่อในเกม**, **เหตุผลที่อยากเข้าร่วม**, และ **ประสบการณ์/ข้อมูลเพิ่มเติม** (ไม่บังคับ)
3. เมื่อกดส่งฟอร์ม บอทจะบันทึกใบสมัครลงตาราง `applications` (สถานะ `รอตรวจสอบ`) แล้วโพสต์สรุปใบสมัครพร้อมปุ่ม ✅ **อนุมัติ** / ❌ **ปฏิเสธ** ไปที่ **ห้องผู้อนุมัติ** ที่ตั้งไว้ใน `applicationChannelId`
4. เฉพาะแอดมิน (role ตาม `adminRoleIds`) เท่านั้นที่กดปุ่มอนุมัติ/ปฏิเสธในห้องผู้อนุมัติได้ — กดซ้ำหรือกดพร้อมกันจะไม่ทำให้อนุมัติซ้ำ (กันด้วยเงื่อนไขสถานะในฐานข้อมูล)
5. ถ้า **อนุมัติ**: บอทจะเพิ่มผู้สมัครเข้าตาราง `members` อัตโนมัติ (ตำแหน่งเริ่มต้นคือตำแหน่งสุดท้ายใน `positions`, แอดมินไปปรับตำแหน่งภายหลังได้ด้วย `/แก้เวลา`/คำสั่งจัดการสมาชิก) อัปเดตห้องรายชื่อ (roster) ให้อัตโนมัติ **แจกยศ (Role) ในดิสคอร์ดให้ผู้สมัครทันที** ตามที่ตั้งไว้ใน `autoRoleIds` (ดูหัวข้อ "แจกยศอัตโนมัติเมื่ออนุมัติใบสมัคร") **และเปลี่ยนชื่อเล่นในดิสคอร์ดเป็นรูปแบบสมาชิกจริง** (ดูหัวข้อ "เปลี่ยนชื่อเล่นอัตโนมัติ")
6. ไม่ว่าอนุมัติหรือปฏิเสธ บอทจะพยายามส่ง DM แจ้งผลให้ผู้สมัครทราบ (ถ้าผู้สมัครปิดรับ DM จะข้ามไปเงียบๆ ไม่ error) และแก้ไขข้อความในห้องผู้อนุมัติให้ปิดปุ่มพร้อมระบุผู้ตัดสิน
7. สมาชิกที่มีใบสมัครค้างอยู่ (`รอตรวจสอบ`) หรือเป็นสมาชิกอยู่แล้วจะไม่สามารถสมัครซ้ำได้ จนกว่าใบเดิมจะถูกตัดสิน

## เปลี่ยนชื่อเล่นอัตโนมัติ (Auto Nickname)

บอทจะเปลี่ยนชื่อเล่น (Nickname) ในดิสคอร์ดของผู้สมัคร **โดยอัตโนมัติ 2 ครั้ง**:

1. **ตอนยื่นใบสมัคร** (กรอกฟอร์มเสร็จ) — เปลี่ยนชื่อเล่นเป็น **ชื่อในเกม** ที่กรอกไว้ทันที (เช่น กรอก "John" → ชื่อเล่นในดิสคอร์ดกลายเป็น `John`)
2. **ตอนอนุมัติใบสมัคร** — เปลี่ยนชื่อเล่นอีกครั้งเป็นรูปแบบสมาชิกจริง `[ตำแหน่ง] ชื่อในเกม` เช่น `[นักเรียนแพทย์] John` (ตำแหน่งเริ่มต้นคือตำแหน่งสุดท้ายใน `positions`)

**ไม่ต้องตั้งค่าอะไรเพิ่มใน `config.json`** — ใช้งานได้ทันที เพราะดึงชื่อมาจากฟอร์มใบสมัครและตำแหน่งเริ่มต้นที่มีอยู่แล้ว

**เงื่อนไข/ข้อจำกัด:**
- บอทต้องมีสิทธิ์ **Manage Nicknames**
- ยศของบอทต้องอยู่ **สูงกว่า** ยศสูงสุดของผู้สมัครคนนั้น (กฎ Role Hierarchy ของ Discord เหมือนกับการแจกยศ)
- Discord ไม่อนุญาตให้บอทใดๆ เปลี่ยนชื่อเล่นของ **เจ้าของเซิร์ฟเวอร์ (Server Owner)** ไม่ว่ากรณีใด — ถ้าเจ้าของเซิร์ฟเวอร์ยื่นใบสมัครเอง บอทจะข้ามการเปลี่ยนชื่อเล่นไปแต่ขั้นตอนอื่นทำงานตามปกติ
- ชื่อเล่นยาวได้ไม่เกิน 32 ตัวอักษร (ตามข้อจำกัดของ Discord) ถ้ารูปแบบ `[ตำแหน่ง] ชื่อในเกม` ยาวเกิน บอทจะตัดท้ายให้พอดี 32 ตัวอักษร
- ถ้าเปลี่ยนชื่อเล่นไม่สำเร็จ (เหตุผลใดก็ตาม) จะ**ไม่ทำให้การสมัคร/การอนุมัติล้มเหลว** แต่จะมีข้อความแจ้งเตือนส่งไปที่ห้อง `logChannels.แอดมิน` ให้แอดมินไปเปลี่ยนเอง

## เปลี่ยนยศอัตโนมัติเมื่อแก้ไขตำแหน่ง (Position Role Sync)

เมื่อแอดมินใช้คำสั่ง `/แก้ไขตำแหน่ง` เปลี่ยนตำแหน่งของสมาชิก บอทจะ **ถอดยศของตำแหน่งเดิม และใส่ยศของตำแหน่งใหม่ให้อัตโนมัติ** ตาม mapping ที่ตั้งไว้ใน `config.json` ที่ `positionRoleIds`

**วิธีตั้งค่า:**
```json
"positionRoleIds": {
  "ผู้อำนวยการเเพทย์": "1532468585270870135",
  "รองผู้อำนวยการเเพทย": "1532468585270870132",
  "เลขานุการ": "1532468585270870131",
  "หัวหน้าแพทย์": "1532468585258025099",
  "แพทย์ชำนาญการพิเศษ": "1536337659134476288",
  "เเพทย์ชำนาญการ": "1532468585258025093",
  "แพทย์บรรจุ": "1532468585258025092",
  "นักเรียนเเพทย์": "1532468585258025091"
}
```
- **key ต้องสะกดตรงกับค่าใน `positions` เป๊ะทุกตัวอักษร** (รวมช่องว่าง/สระ) เพราะบอทใช้ชื่อตำแหน่งเป็นตัวค้นหายศ
- ถ้าตำแหน่งไหนไม่ได้ตั้งไว้ใน `positionRoleIds` บอทจะข้ามการเปลี่ยนยศของตำแหน่งนั้นไปเงียบๆ

**พฤติกรรม:**
- ถ้าตำแหน่งเดิมกับตำแหน่งใหม่ mapping ไปยศเดียวกัน จะไม่ถอด/ใส่ซ้ำ
- ถ้าสมาชิกไม่ได้ถือยศตำแหน่งเดิมอยู่แล้ว (เช่นถูกถอดออกไปเอง) จะข้ามขั้นตอนถอด แล้วใส่ยศใหม่ตามปกติ
- เงื่อนไข/ข้อจำกัดเหมือนกับ "แจกยศอัตโนมัติเมื่ออนุมัติใบสมัคร" ด้านล่าง (บอทต้องมีสิทธิ์ Manage Roles และยศบอทต้องอยู่สูงกว่ายศเป้าหมายทั้งหมด)
- ถ้าเปลี่ยนยศไม่สำเร็จ จะไม่ทำให้การเปลี่ยนตำแหน่งในฐานข้อมูลล้มเหลว แต่จะแจ้งเตือนไปห้อง log `logChannels.แอดมิน`

## แจกยศอัตโนมัติเมื่ออนุมัติใบสมัคร (Auto Role)

เมื่อแอดมินกด ✅ **อนุมัติ** ใบสมัคร บอทจะแจกยศ (Role) ในดิสคอร์ดให้ผู้สมัคร **ทันที** ตามรายชื่อยศที่ตั้งไว้ใน `config.json` ที่ `autoRoleIds` — ใส่กี่ยศก็ได้ ไม่จำกัดแค่ 2 ยศ

**วิธีตั้งค่า:**
1. เปิดเซิร์ฟเวอร์ > Server Settings > Roles > คลิกขวายศที่ต้องการ > Copy Role ID (ต้องเปิด Developer Mode ก่อน)
2. ใส่ Role ID ลงใน `config.json`:
   ```json
   "autoRoleIds": [
     "1234567890123456",
     "9876543210987654"
   ]
   ```
3. **สำคัญ**: ยศของบอท (Bot Role) ในเซิร์ฟเวอร์ต้องอยู่ **สูงกว่า** ยศทั้ง 2 ที่ต้องการแจก ไม่งั้น Discord จะไม่ให้บอทมอบยศนั้น (ไปที่ Server Settings > Roles แล้วลากยศบอทให้อยู่บนสุด/สูงกว่ายศเป้าหมาย)
4. บอทต้องมีสิทธิ์ **Manage Roles** (ตอนเชิญบอทเข้าเซิร์ฟเวอร์ให้ติ๊ก permission นี้ไว้ด้วย)

**พฤติกรรม:**
- แจกยศเฉพาะตอน **อนุมัติใบสมัครครั้งแรก** เท่านั้น (ถ้าผู้สมัครเป็นสมาชิกอยู่แล้วในระบบจะไม่แจกซ้ำ)
- ถ้าไม่ได้ตั้งค่า `autoRoleIds` (ปล่อยว่างหรือเป็น `"ใส่_ROLE_ID_..."`) บอทจะข้ามการแจกยศไปเงียบๆ ไม่กระทบการอนุมัติ
- ถ้าแจกยศไม่สำเร็จ (บอทไม่มีสิทธิ์, ยศบอทอยู่ต่ำกว่ายศเป้าหมาย, หรือผู้สมัครออกจากเซิร์ฟเวอร์ไปแล้ว) การอนุมัติใบสมัคร/เพิ่มสมาชิกในระบบจะ**ไม่ล้มเหลวตาม** แต่จะมีข้อความแจ้งเตือนส่งไปที่ห้อง `logChannels.แอดมิน` ให้แอดมินไปแจกยศเองด้วยมือ
- ผลการแจกยศ (สำเร็จ/ไม่สำเร็จ และเหตุผล) จะถูกบันทึกลง log ห้อง `logChannels.ใบสมัคร` ด้วยทุกครั้ง

## ระบบ Log เข้าเวร/ออกเวร

ทุกครั้งที่มีการเข้าเวร/ออกเวร ไม่ว่าจะผ่านการ**กดปุ่มบนแผงเข้าเวร**หรือพิมพ์คำสั่ง `/เข้าเวร` `/ออกเวร` บอทจะส่ง log แบบเดียวกันไปที่ห้อง log ที่ตั้งไว้เสมอ เพื่อให้แอดมินตรวจสอบย้อนหลังได้ง่าย

**วิธีเปิดใช้งาน:**
1. สร้างห้องข้อความสำหรับเก็บ log (เช่น `#log-เข้าเวร`) แล้ว copy Channel ID
2. ใส่ Channel ID ลงใน `config.json` ที่ `logChannels.เข้าเวร` และ `logChannels.ออกเวร` (จะใช้ห้องเดียวกันหรือแยกห้องกันก็ได้)
   ```json
   "logChannels": {
     "สมัคร": "1234567890",
     "เข้าเวร": "1234567890",
     "ออกเวร": "1234567890",
     "แอดมิน": "1234567890"
   }
   ```
3. ถ้าไม่ใส่ค่า (ปล่อยเป็น `"ใส่_CHANNEL_ID"`) บอทจะข้ามการส่ง log ห้องนั้นไปเงียบๆ ไม่ error

**ข้อมูลที่อยู่ใน log แต่ละรายการ:**
- รูปโปรไฟล์ + ชื่อ Discord ของผู้ทำรายการ (author ของ embed)
- ชื่อในเกม, หน่วยงาน, เวลาที่เข้า/ออกเวร, จำนวนชั่วโมงรวม (กรณีออกเวร)
- ผู้ใช้แบบ mention (`<@id>`) กดแล้วเด้งไปหาโปรไฟล์ได้ทันที
- footer บอกช่องทางที่ใช้ทำรายการ เช่น `ผ่าน: ปุ่มแผงเข้าเวร` หรือ `ผ่าน: คำสั่ง /เข้าเวร` เพื่อแยกให้ชัดว่าทำผ่านปุ่มหรือพิมพ์คำสั่ง

นอกจากนี้ยังมี:
- `/สมัคร` → log เข้าห้อง `logChannels.สมัคร`
- คำสั่งแอดมินทุกตัว (`/ล้างเวร`, `/แก้เวลา`, `/เพิ่มชั่วโมง`, `/ลดชั่วโมง`) → log เข้าห้อง `logChannels.แอดมิน` พร้อมชื่อแอดมินที่ดำเนินการ
- `/ส่งออกข้อมูล` → export log ทั้งหมดในตาราง `duty_log` เป็นไฟล์ `.csv` แนบในดิสคอร์ด สำหรับดูประวัติย้อนหลังแบบละเอียดหรือทำรายงาน

## หลักการออกแบบที่ควรรู้

- **ป้องกันเข้า/ออกเวรซ้ำ**: เช็คสถานะล่าสุดใน `duty_log` ก่อนทุกครั้ง (query `WHERE discord_id = ? AND checkout_at IS NULL`)
- **รองรับข้ามวัน**: ชั่วโมงคำนวณจาก timestamp เต็ม (ISO datetime) ไม่ใช่แค่เวลานาฬิกา จึงข้ามเที่ยงคืนได้ถูกต้อง
- **ชั่วโมงวันนี้/สัปดาห์/เดือน**: คำนวณสดจาก `duty_log` ทุกครั้งที่ใช้ `/ชั่วโมง` ด้วย SQL query ตรง เพื่อความแม่นยำสูงสุด ส่วนตาราง `summary` จะถูกอัปเดตเมื่อแอดมินรัน `/สรุปสัปดาห์` หรือสั่งเคลียร์ฐานข้อมูลรายสัปดาห์ด้วยตนเองผ่าน `/เคลียร์ฐานข้อมูลรายสัปดาห์`

### ระบบเคลียร์ฐานข้อมูลรายสัปดาห์ — สั่งเอง ไม่มีระบบอัตโนมัติ (`src/utils/weeklyReset.js`)

- **ไม่มีการรันอัตโนมัติอีกต่อไป** แอดมินเป็นคนตัดสินใจเองว่าเมื่อไหร่ถือว่า "จบรอบ" แล้วสั่งเคลียร์ผ่านคำสั่ง `/เคลียร์ฐานข้อมูลรายสัปดาห์ ยืนยัน:true` หรือปุ่ม 🧹 ในแผงแอดมิน (ต้องกดยืนยันอีกครั้งก่อนทำงานจริง เพราะเป็นการลบข้อมูล)
- เมื่อสั่งรัน ระบบจะ: (1) สรุปยอดชั่วโมงจากข้อมูลที่ค้างอยู่ใน `duty_log` ตอนนี้ (2) บวกสะสมเข้ากับประวัติของสัปดาห์ปัจจุบันในตาราง `weekly_summary_history` — ถ้าสั่งเคลียร์หลายครั้งในสัปดาห์เดียวกันยอดจะสะสมรวมกัน ไม่ถูกเขียนทับ (3) อัปเดตตาราง `summary` ด้วยยอดสะสมล่าสุด (4) **ลบแถวใน `duty_log` ที่ปิดรายการแล้วออกจากฐานข้อมูลจริง** (ออกเวร/ล้างแล้ว/ปรับเพิ่ม/ปรับลด — ไม่ลบแถวที่ยังเข้าเวรค้างอยู่) (5) โพสต์แจ้งสรุปในห้องตาม `logChannels["สรุปสัปดาห์"]` ในไฟล์ `config.json`
- **ผลข้างเคียงที่ควรรู้**: `/ชั่วโมง` คำนวณ hoursToday/hoursWeek/hoursMonth สดจาก `duty_log` ตารางเดียวกัน ดังนั้นการเคลียร์จะทำให้ยอดวันนี้/เดือนนี้ของทุกคนเริ่มนับใหม่ไปด้วย ไม่ใช่แค่ยอดสัปดาห์ จึงควรสั่งเคลียร์ตอนจบรอบสัปดาห์จริงๆ เท่านั้น — ยอดที่สรุปไปแล้วยังดูย้อนหลังได้เสมอผ่าน `/ประวัติสัปดาห์`
- **การปรับชั่วโมงมือ**: `/เพิ่มชั่วโมง` และ `/ลดชั่วโมง` จะเพิ่มแถวพิเศษใน `duty_log` (สถานะ `ปรับเพิ่ม`/`ปรับลด`) เพื่อให้นับรวมในสรุปโดยไม่ต้องแก้ยอดตรงๆ — ถ้ายังไม่ได้เคลียร์ ก็ยังตรวจสอบย้อนหลังได้ง่ายผ่าน query
- **หลายหน่วยงาน**: กำหนดผ่าน `departments` ใน `config.json` จะกลายเป็นตัวเลือก (choices) ในคำสั่ง `/สมัคร` อัตโนมัติ
- **สำรองข้อมูล**: เนื่องจากไม่มี Google Sheets ให้เปิดดูสดๆ อีกแล้ว แนะนำให้ตั้ง cron/scheduled task รันคำสั่งสำรอง (backup) ไฟล์ `data/duty.db` เป็นระยะ หรือใช้ `/ส่งออกข้อมูล` เพื่อดึงข้อมูลออกมาดูเป็นระยะ

## ต่อยอดในอนาคต (ตามสเปก)

โครงสร้างปัจจุบันรองรับการเพิ่มระบบลา/อนุมัติได้ง่าย โดยแนะนำให้:
1. เพิ่มตาราง `leave_requests` ใน `utils/db.js`
2. เพิ่มคำสั่ง `/ลา`, `/อนุมัติ`, `/ปฏิเสธ` ในรูปแบบเดียวกับโค้ดที่มีอยู่
3. ใช้ `utils/permissions.js` และ `utils/embeds.js` ชุดเดิมได้ทันที

## หมายเหตุด้านความปลอดภัย

- ห้าม commit ไฟล์ `.env` และ `data/duty.db` ขึ้น git (มี `.gitignore` เตรียมไว้ให้แล้ว) เพราะไฟล์ฐานข้อมูลมีข้อมูลสมาชิกจริง
- แนะนำให้สำรอง (backup) ไฟล์ `data/duty.db` เป็นระยะ เนื่องจากข้อมูลทั้งหมดอยู่ในเครื่องที่รันบอทเท่านั้น ไม่มีสำเนาบนคลาวด์เหมือน Google Sheets เดิม
